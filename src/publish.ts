import { Context } from "semantic-release";

import type PluginConfig from "./types/pluginConfig";

const publish = (pluginConfig: PluginConfig, context: Context): void => {
  // publish logic
};

export default publish;
